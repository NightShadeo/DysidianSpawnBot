services:
  - type: web
    name: DysidianSpawnBot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      PYTHON_VERSION: 3.11.9